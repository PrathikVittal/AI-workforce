# Local SQLite database (libSQL). Leave as-is for zero-setup local dev.
DATABASE_URL=file:./scrum.db

# OPTIONAL. Without this, the lead agent uses a built-in heuristic extractor
# so the whole pipeline still runs for free. Add a key to get real GPT-4o-mini
# task extraction. Get one at https://platform.openai.com
# OPENAI_API_KEY=sk-...
